# 生产 override 会固定 NODE_ENV=production；保留此项也能让误用基础 Compose 时安全失败。
NODE_ENV=production

# 故意留空：复制为 .env 后生成并持久保存至少 32 个字符的密钥，例如：openssl rand -hex 32
# docker-compose.production.yml 会在该值为空时拒绝解析，后端会拒绝长度不足的值。
JWT_SECRET=
JWT_EXPIRES_IN=168h

# 仅当前后端不同源时填写，多个来源使用英文逗号分隔。
CORS_ALLOWED_ORIGINS=
