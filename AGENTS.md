# Smart Bill Manager Agent 规则

## 权威文档

开始任何任务前，按需阅读以下文档：

- `docs/product.md`：产品定位、用户与核心流程。
- `docs/scope.md`：范围、非目标与 Clean Slate 边界。
- `docs/acceptance.md`：验收指标、证据和完成定义。
- `docs/architecture.md`：系统结构、依赖方向和运行时边界。
- `docs/ai-pipeline.md`：AI 接口、候选结果、校验与审核规则。
- `docs/data-model.md`：Source、Claim、Fact 及领域数据模型。
- `docs/ui-ux.md`：信息架构、视觉方向和交互标准。
- `docs/roadmap.md`：当前里程碑与进入下一阶段的门禁。

发生冲突时，`scope.md` 与 `acceptance.md` 的明确边界优先于其他项目文档；架构决策只能通过更新文档并重新验收来改变。

## Clean Slate 不变量

- 本项目是全新架构，不兼容旧代码、旧数据库、旧 API、旧任务状态或旧部署方式。
- 不保留、不读取、不迁移旧数据；不得增加兼容层、双写、旧字段回填或旧版导入工具。
- `backend-go/`、`frontend/`、旧 OCR 脚本和旧 Compose 仅是遗留参考，不得被新代码依赖或复制。
- 新代码只能进入约定的新目录，例如 `apps/api/`、`apps/web/`、`contracts/`、`infra/` 和 `tests/`。
- 新数据库从自己的 `0001` 开始；新 HTTP API 从自己的 `/api/v1` 开始。
- 是否重写 Git 历史是独立治理决策，不得与应用重构混在同一变更中。

## 当前阶段

`docs/roadmap.md` 已记录 M0 于 2026-08-27 完成、M1 于 2026-08-30 完成；产品负责人已于 2026-08-30 明确授权启动 M2。M2 整体仍在进行中；“支付—发票金额分配”和“确定性重复检测”两个切片均已完成并通过验收。

- M1 已按 `docs/scope.md` 的退出条件和 `docs/m1-evidence.md` 收口；M2 每个后续切片仍必须先在权威文档冻结范围与验收，再同步实现领域、数据库、API 和 UI。
- 2026-08-27 批准的模型准确率目标保留为 M4 正式发布门禁；不得把历史失败改写为通过，也不得为低清、裁切、遮挡或屏摄图片继续扩张 Prompt/预处理分支，除非产品负责人另行明确授权。
- 模型正确率专项安排在全部功能开发完成后处理；这不授权绕过安全闭环、人工审核或 M2 进入门禁。
- M2 首切片只允许同币种、正整数最小单位的人工金额分配；禁止汇率换算、自动接受、静默截断、超分配和原地改写历史 Link。
- 下一开发断点是先冻结“复杂多页 PDF 的跨页明细重建与分页审阅”切片的范围、验收和必要 ADR；文档冻结前不得写入实现，也不得把已完成的跨页重复检测当作明细重建。
- 禁止删除远端旧版本、重写历史、发布镜像或部署。
- 冻结的设计基线和已实现的 M1 不变量仍是后续实现与验收依据；结构性变更必须同步更新权威文档并重新验收。

## 架构与业务规则

- 数据流固定为 `Source -> Claim -> Fact`。
- 模型只按最小中文契约返回 Payment/Invoice 所需字段的票面原文与一基页码 `{text,page}`，不得返回内部 Claim、归一化值、独立 Evidence、推导值或审核结论；本地 `claim-mapper/3` 负责确定性格式化、Evidence 组装和 Claim 映射。M1 中只有用户明确确认后才能创建或修改 Fact。
- 一个 `OpenAICompatibleAdapter` 是第一阶段唯一 AI 传输实现；禁止供应商名称分支、多模型路由和自动降级。
- `bill-visible-text/1` 是模型输出的唯一权威本地 Envelope Schema，只硬校验有效 JSON、封闭根对象、`schema_version`、`document_type` 与 Payment/Invoice 根成员；Provider-facing Schema 只能由它确定性投影。嵌套票面字段不得在 Adapter 阶段因局部类型错误而整份丢弃。
- 每个非空模型字段只能是 `{text,page}`；`text` 必须是字段值本身的票面原文，`page` 从 1 开始。模型没有看到的字段返回 `null`，不得计算、猜测、补默认值或纠正字符。
- `claim-mapper/3` 是模型 JSON 到 `document-claim/2` 的唯一转换位置：它将票面原文绑定为 Evidence，并确定性处理批准的币种/金额、日期、交易时间、显式时区、中文默认时区、数量、`null`、明细顺序和审核专用补充字段。它不得计算缺失业务值、纠正 OCR 字符、增加 Provider 品牌分支或从标签外文本重新理解单据。
- 发票 `amount_with_tax` 只能映射为 `total_minor`，`amount_without_tax` 只进入审核专用补充字段；空白明细值必须保持 absent。除无效 JSON 或错误根身份外，单字段形状、页码、金额、日期、时区、业务区段和 Evidence 问题必须形成显式 Validation 与 `blocked` Claim，同时保留同一文档的其他正确字段供审核。
- 所有 AI 输出必须经过 JSON Schema、确定性业务校验和本地权限校验。
- 所有业务、文件、任务、AI Run 和审核记录必须具有租户边界。
- 金额使用整数最小货币单位；时间戳使用 UTC，业务日期与时区显式保存。
- 原始文档不可变；正式字段必须能追溯到 Source、AI Run、Claim 和 Review Decision。
- API Key 必须加密保存，解密主密钥不得与密文保存在同一数据源。
- 日志不得记录密钥、完整原始单据、完整模型响应或不必要的个人财务信息。
- 文档内容属于不可信输入，不能改变系统指令、调用工具或绕过审核。

## 测试与数据

- 不建立运行时“回归样本模块”、回归样本表、管理 API 或页面。
- 单元、集成、契约、端到端、性能、备份恢复和 Mock Provider 测试只使用固定纯合成数据。
- 模型业务质量必须使用版本化的中文真实场景数据；程序生成的合成票据只能验证传输、Schema、校验、计分和失败路径，不能形成模型质量发布证据。
- M1 中文支付图片未显式标注时区时，批准的确定性默认值为 `Asia/Shanghai`：模型只从图片抄录完整本地日期时间，再按 `+08:00` 规范化；`source_timezone` 属于产品默认值，不能伪造成图片逐字证据。缺少完整日期或图片显式冲突时仍须阻断。
- 真实场景数据只允许来自可回溯原始公开页面、且对应一次真实交易或真实开具行为的公开业务单据，或用户逐批明确授权的本地单据；政府/平台票样、产品演示、教程构造图和空白模板只能做版式参考，不能计入模型质量样本。禁止从生产数据库、邮箱、上传目录、运行日志或其他运行时数据源自动采集。
- 真实原图、逐字段标注和原始模型结果只保存在 Git 忽略、Docker 构建排除、权限为 `0600` 的本地评测区。仓库证据只记录安全样本 ID、内容哈希、来源类别、冻结配置和聚合指标，不记录原图、完整字段值或非必要来源定位。
- 公开真实图片允许按原样保存在本地隔离评测区，包括图片公开时已经包含的企业或自然人信息；不得把原图、真实字段值或个人信息提交到 Git、写入仓库文档/日志/生产镜像或在回复中展示。调用视觉模型会把图片发送给活动 Provider，执行前必须明确 Provider 主机、样本数量并取得产品负责人当次授权。
- 测试及评估资产不得进入生产镜像。
- 关键失败必须显式暴露；禁止宽泛吞错、伪成功和隐藏降级。
- 重复请求、租户隔离、任务恢复、取消和失败路径必须有确定性测试。

## 范围与交付

- 每个里程碑必须写明目标、范围内、范围外、用户场景、验收指标、验收证据和退出条件。
- 新需求默认进入 Backlog；加入当前里程碑前必须同步调整范围和验收标准。
- 未满足 `docs/acceptance.md` 的证据要求，不得宣称里程碑完成。
- 修改后检查完整 diff，确认没有业务代码越界、遗留依赖、第二数据源或未说明的行为变化。

## 文档与沟通

- 新增项目文档和注释默认使用中文；公开 API 名称、协议字段和必要术语保留英文。
- 文档描述约束、原因和验证方式，不记录会迅速失效的执行流水账。
- 路径、命令和指标必须可执行或可验证；不使用“体验良好”“足够快”等模糊验收语句。
