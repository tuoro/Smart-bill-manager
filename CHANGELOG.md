# 变更日志

本项目从 `v0.2.0` 起按语义化版本记录面向使用者和维护者的重要变化。

## [0.3.1] - 2026-09-01

### 自托管实测分发

- 发布固定内容摘要的 `linux/amd64` GHCR 镜像，并提供基于当前 Clean Slate Compose 契约的发布 overlay。
- 新增 owner-only 凭据准备器和部署命令，覆盖拉取、PostgreSQL provision/migration、唯一 Owner 初始化、启动、状态、日志、停止和保留数据卷的 down。
- 新增面向用户的单机部署指南；默认只绑定 `127.0.0.1`，固定 PostgreSQL 17 镜像摘要，不提供隐式数据卷删除。
- 重写中英文 README，把内部里程碑流水移至 `docs/`，首页聚焦产品、快速部署、限制、安全边界和文档入口。

### 版本边界

- `v0.3.0` Git Tag 保持不可变；`v0.3.1` 是补齐部署能力的补丁预发布，应用业务实现和 M4 发布输入摘要不变。
- 真实模型正确率、真实邮箱/Provider 联调、TLS/域名和生产部署仍未完成。

## [0.3.0] - 2026-09-01

### Clean Slate 新架构

- 交付 M0～M4 本地产品功能：AI 收件箱、人工审核、Payment/Invoice Fact、金额分配、重复检测、跨页明细、批量上传、邮件附件、行程、报销和确定性洞察。
- PostgreSQL 17 成为唯一关系数据源；新系统不读取或迁移 SQLite 及 `v0.x` 旧数据，也不保留旧 API、双写或兼容入口。
- AI 链路固定为原始图片直达多模态模型、最小中文指令提取票面文本、本地确定性规范化与 JSON/Claim 组装；模型不能直接创建 Fact。

### 本地发布准备

- 提供单一受限 Compose 运行入口、最小权限 PostgreSQL 角色、显式迁移、Owner 初始化、健康检查和认证备份恢复工具。
- 本地门禁已覆盖 M1～M4 不变量、10,000 Fact 性能、内存稳定、1,000 Document 恢复、浏览器流程、响应式、可访问性和发布镜像安全检查。
- 本版本作为新架构公开实测预发布版发布。真实模型正确率正式评测、真实邮箱或 Provider 联调及生产部署尚未完成，不应视为生产稳定版。

### 重要边界

- `v0.3.0` 必须使用全新数据库和对象存储；不支持从 `v0.2.4` 或更早版本原地升级。
- 仓库内旧 `backend-go/`、`frontend/` 与旧部署文件只保留为历史参考，不是 `v0.3.0` 运行入口。

## [0.2.4] - 2026-08-10

### 数据迁移与密钥一致性

- 启动流程会在修改业务 schema 前检查已有 `schema_migrations`，旧程序遇到未来数据库版本时直接拒绝启动；版本化迁移继续各自在独立事务中执行，失败时不写入迁移记录。
- 新增邮件日志身份迁移：按所有者、邮箱配置、邮箱目录和消息 UID 识别重复项，保留质量最高的记录，并无损合并标量/列表形式的全部发票关联及缺失元数据后才删除冗余副本；无效历史 JSON、更新行数异常、删除失败或索引校验失败都会回滚整个迁移。
- 历史邮箱明文密码修复改为单事务执行，并在写入任何新密文前用当前密钥验证全部已有密文；已有密文但密钥缺失、损坏或不匹配时阻止启动，不会生成替代密钥或留下明密文混合状态。

### 服务边界与 OCR 停机

- 仪表盘近期支付与发票计数下沉到支付服务和仓储，统一执行所有者、草稿和关联发票边界；行程导出的空结果使用可识别领域错误，handler 不再直接查询数据库或匹配错误字符串。
- 支付与发票服务共享由应用注入并持有的 OCR worker；worker 启动、识别和关闭纳入同一应用生命周期，避免并行实例和跨层全局状态。
- OCR worker 使用明确的终态状态机和串行请求闸门；关闭会拒绝新任务，中断排队、启动、管道读写与自动重启，终止子进程并只等待回收一次；worker 模式的 CLI 回退与同一终态锁和关闭信号绑定，关闭、请求取消或超时后不会再回退启动 CLI，已在途的 CLI 会立即取消并纳入停机回收等待，应用侧停机等待有明确超时。

### 依赖与生产交付

- 前端锁文件将传递依赖 `nanoid` 从 3.3.16 更新到 3.3.18，恢复 `npm audit --audit-level=high` 安全门禁；发布验证继续执行零警告 lint、58 项测试和生产构建。
- 新增 `docker-compose.production.yml` override，固定 `NODE_ENV=production` 并在 `JWT_SECRET` 为空时拒绝解析；后端继续验证密钥至少 32 个字符，`.env.example` 故意不提供默认密钥。
- README 区分本地基础 Compose 与生产 override，并修正 v0.1.0 升级顺序：拉取新代码前只能使用旧版基础 Compose 停机和备份，拉取后再准备持久密钥、校验生产配置并启动。

### 升级注意

- 本版本不包含删表、删列或丢弃业务关联的破坏性数据库迁移；邮件日志冗余行只在全部关联和可保留元数据合并成功后于同一事务中移除。
- 从 v0.1.0 升级前必须备份 `bills.db`、`email_password.key` 和上传目录并验证备份可读；迁移不会创建外部备份，回退仍需恢复已验证的备份。
- 拉取 v0.2.4 后创建或更新 `.env`：已有持久 `JWT_SECRET` 必须沿用，否则生成一次至少 32 个字符的新密钥；随后使用基础 Compose 与 production override 共同执行 `config --quiet` 和启动。
- `email_password.key` 或外部邮箱密码密钥必须与数据库中的已有密文匹配；密钥丢失或更换会阻止启动，需先恢复正确密钥。

## [0.2.3] - 2026-08-03

### 行程日历正确性

- 修复 PrimeVue 月份切换事件使用 1 基、日期插槽使用 0 基，却共用自动推断状态导致的月份偏移。
- 月份事件和日期插槽改用各自明确的转换路径；年份切换保留当前可见月份，规避组件不同交互路径返回不一致的月份基数。
- 新增月份边界与异常值测试，前端测试增至 58 项；本版本不包含数据库迁移。

## [0.2.2] - 2026-08-03

### 前端类型与生命周期

- 清理邮箱监控和行程页面剩余的 56 处显式 `any` 与 6 处无效正则转义，前端历史 ESLint 抑制由最初 103 处降为 0。
- 使用 PrimeVue 分页事件、日期槽和最小运行时实例契约替代宽泛断言，并为 Temporal 时区转换与行程写入响应补齐准确类型。
- 日期面板在页面卸载时清理全局滚动、缩放监听和延迟动画帧，避免残留 DOM 与组件引用。

### 下载与错误边界

- 邮件和行程导出接口统一声明 `Blob` 响应，移除调用端响应体断言。
- 提取共享 `Content-Disposition` 文件名解析与净化工具，兼容引号、普通和 UTF-8 扩展文件名，并过滤危险路径字符。
- 统一邮箱与行程页面的结构化 API 错误和请求超时识别，保留取消请求语义。

### 测试与交付

- 前端共享层测试增至 50 项，覆盖超时识别、下载文件名解析、响应头收窄和文件名净化。
- 严格 lint、类型检查、生产构建和依赖安全审计均通过；本版本不包含数据库迁移。
- 后端代码未变，CI 继续执行完整 CGO 测试、`go vet` 和 35% 整体覆盖率门槛。

## [0.2.1] - 2026-08-03

### 安全与正确性

- 增加真实路由与 SQLite 装配的 HTTP 契约测试，覆盖初始化、登录、邀请码、权限、停用账号和管理员代操作二次确认。
- 统一发票预览、下载、附件下载与支付截图的文件路径解析，阻断目录穿越、绝对路径和符号链接逃逸。
- 验证支付金额按整数分持久化且 API 往返无精度漂移，并覆盖支付记录的跨用户读写删除隔离。
- 收紧发票与支付关联的未找到语义，跨用户关联返回 404 且事务不产生部分写入。

### 前端质量

- 为异步任务 DTO 和轮询流程增加结果泛型，明确支付与发票 OCR 任务契约。
- 统一结构化 API 错误解析，修复日志取消请求误报，并移除通知面板对 PrimeVue 私有状态的依赖。
- 清理认证组件、后台页面及账单核心页面的 47 处显式 `any` 抑制，遗留数量由 103 降至 56。

### 测试与交付

- 前端共享层测试增至 37 项，严格 lint、类型检查和生产构建纳入持续验证。
- 后端整体语句覆盖率为 35.9%，CI 最低防回退门槛由 25% 提高到 35%。
- 本版本不包含新的数据库迁移，升级时可直接沿用 v0.2.0 的数据与上传持久卷。

## [0.2.0] - 2026-08-03

### 数据正确性

- 建立带 `schema_migrations` 记录的版本化数据库迁移，迁移失败时拒绝启动。
- 回填旧数据所有者、时间戳、发票日期和 OCR 拆分数据，补齐查询索引。
- 支付、发票和税额改用整数分字段参与持久化、聚合和筛选；兼容期继续双写旧元字段。
- 修复跨用户发票去重和关联查询边界。

### 后端架构

- 拆分配置、数据库、迁移、应用装配和运行生命周期，支持优雅停机。
- 移除全局数据库状态，为认证、支付、发票、行程和邮箱领域显式注入依赖。
- 为支付与发票仓储增加事务绑定，收紧 OCR 更新、关联、草稿清理和文件删除的提交顺序。
- 统一 API 错误边界：5xx 不再向客户端泄露内部错误详情。
- 生产环境强制校验 JWT 密钥和 CORS 配置。

### 前端架构

- 拆分请求客户端、本地持久化和领域 API，统一超时、并发、401 与代操作确认。
- 使用 Pinia 统一认证会话，登录、注册、初始化和退出采用一致的状态写入规则。
- 提取异步任务轮询和分页列表 composable，处理取消、竞态和过期响应。
- 拆分支付详情领域组件，降低支付页面职责。
- 收紧共享工具、通知和任务结果类型，前端 ESLint 达到零警告。

### 测试与交付

- 增加迁移、金额、事务、认证、配置、应用生命周期及前端共享层测试。
- CI 执行 CGO 测试、`go vet`、前端依赖审计、lint、Vitest、构建和 Docker 验证。
- 后端整体语句覆盖率为 35.8%，最低防回退门槛设为 25%。

### 升级注意

- 从 v0.1.0 升级会在首次启动时自动执行数据库迁移，启动耗时取决于历史支付和发票数量。
- 升级前必须备份数据库、`email_password.key` 和上传目录；迁移本身不会创建外部备份。
- 生产环境必须提供至少 32 个字符的持久 `JWT_SECRET`。

## [0.1.0] - 2026-08-03

- 建立渐进式重构前的稳定基线版本。
- 提供支付、发票、邮箱、行程、多用户和异步 OCR 的既有功能。
- 保存可回退 Git 标签和 GHCR 容器镜像。

[0.3.1]: https://github.com/tuoro/Smart-bill-manager/compare/v0.3.0...v0.3.1
[0.3.0]: https://github.com/tuoro/Smart-bill-manager/compare/v0.2.4...v0.3.0
[0.2.4]: https://github.com/tuoro/Smart-bill-manager/compare/v0.2.3...v0.2.4
[0.2.3]: https://github.com/tuoro/Smart-bill-manager/compare/v0.2.2...v0.2.3
[0.2.2]: https://github.com/tuoro/Smart-bill-manager/compare/v0.2.1...v0.2.2
[0.2.1]: https://github.com/tuoro/Smart-bill-manager/compare/v0.2.0...v0.2.1
[0.2.0]: https://github.com/tuoro/Smart-bill-manager/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/tuoro/Smart-bill-manager/releases/tag/v0.1.0
