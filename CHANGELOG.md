# 变更日志

本项目从 `v0.2.0` 起按语义化版本记录面向使用者和维护者的重要变化。

## [0.2.2] - 2026-08-03

### 前端类型与生命周期

- 清理邮箱监控和行程页面剩余的 56 处显式 `any` 与 6 处无效正则转义，前端历史 ESLint 抑制由最初 103 处降为 0。
- 使用 PrimeVue 分页事件、日期槽和最小运行时实例契约替代宽泛断言，并为 Temporal 时区转换与行程写入响应补齐准确类型。
- 日期面板在页面卸载时清理全局滚动、缩放监听和延迟动画帧，避免残留 DOM 与组件引用。

### 下载与错误边界

- 邮件、行程和回归样本导出接口统一声明 `Blob` 响应，移除调用端响应体断言。
- 提取共享 `Content-Disposition` 文件名解析与净化工具，兼容引号、普通和 UTF-8 扩展文件名，并过滤危险路径字符。
- 统一邮箱与行程页面的结构化 API 错误和请求超时识别，保留取消请求语义。

### 测试与交付

- 前端共享层测试增至 50 项，覆盖超时识别、下载文件名解析、响应头收窄和文件名净化。
- 严格 lint、类型检查、生产构建和依赖安全审计均通过；本版本不包含数据库迁移。
- 后端代码未变，CI 继续执行完整 CGO 测试、`go vet` 和 35% 整体覆盖率门槛。

## [0.2.1] - 2026-08-03

### 安全与正确性

- 增加真实路由与 SQLite 装配的 HTTP 契约测试，覆盖初始化、登录、邀请码、权限、停用账号和管理员代操作二次确认。
- 统一发票预览、下载、附件下载与支付截图的文件路径解析，阻断目录穿越、绝对路径和符号链接逃逸。
- 验证支付金额按整数分持久化且 API 往返无精度漂移，并覆盖支付记录的跨用户读写删除隔离。
- 收紧发票与支付关联的未找到语义，跨用户关联返回 404 且事务不产生部分写入。

### 前端质量

- 为异步任务 DTO 和轮询流程增加结果泛型，明确支付与发票 OCR 任务契约。
- 统一结构化 API 错误解析，修复日志取消请求误报，并移除通知面板对 PrimeVue 私有状态的依赖。
- 清理认证组件、后台页面及账单核心页面的 47 处显式 `any` 抑制，遗留数量由 103 降至 56。

### 测试与交付

- 前端共享层测试增至 37 项，严格 lint、类型检查和生产构建纳入持续验证。
- 后端整体语句覆盖率为 35.9%，CI 最低防回退门槛由 25% 提高到 35%。
- 本版本不包含新的数据库迁移，升级时可直接沿用 v0.2.0 的数据与上传持久卷。

## [0.2.0] - 2026-08-03

### 数据正确性

- 建立带 `schema_migrations` 记录的版本化数据库迁移，迁移失败时拒绝启动。
- 回填旧数据所有者、时间戳、发票日期和 OCR 拆分数据，补齐查询索引。
- 支付、发票和税额改用整数分字段参与持久化、聚合和筛选；兼容期继续双写旧元字段。
- 修复跨用户发票去重和关联查询边界。

### 后端架构

- 拆分配置、数据库、迁移、应用装配和运行生命周期，支持优雅停机。
- 移除全局数据库状态，为认证、支付、发票、行程和邮箱领域显式注入依赖。
- 为支付与发票仓储增加事务绑定，收紧 OCR 更新、关联、草稿清理和文件删除的提交顺序。
- 统一 API 错误边界：5xx 不再向客户端泄露内部错误详情。
- 生产环境强制校验 JWT 密钥和 CORS 配置。

### 前端架构

- 拆分请求客户端、本地持久化和领域 API，统一超时、并发、401 与代操作确认。
- 使用 Pinia 统一认证会话，登录、注册、初始化和退出采用一致的状态写入规则。
- 提取异步任务轮询和分页列表 composable，处理取消、竞态和过期响应。
- 拆分支付详情领域组件，降低支付页面职责。
- 收紧共享工具、通知和任务结果类型，前端 ESLint 达到零警告。

### 测试与交付

- 增加迁移、金额、事务、认证、配置、应用生命周期及前端共享层测试。
- CI 执行 CGO 测试、`go vet`、前端依赖审计、lint、Vitest、构建和 Docker 验证。
- 后端整体语句覆盖率为 35.8%，最低防回退门槛设为 25%。

### 升级注意

- 从 v0.1.0 升级会在首次启动时自动执行数据库迁移，启动耗时取决于历史支付和发票数量。
- 升级前必须备份数据库、`email_password.key` 和上传目录；迁移本身不会创建外部备份。
- 生产环境必须提供至少 32 个字符的持久 `JWT_SECRET`。

## [0.1.0] - 2026-08-03

- 建立渐进式重构前的稳定基线版本。
- 提供支付、发票、邮箱、行程、多用户和异步 OCR 的既有功能。
- 保存可回退 Git 标签和 GHCR 容器镜像。

[0.2.2]: https://github.com/tuoro/Smart-bill-manager/compare/v0.2.1...v0.2.2
[0.2.1]: https://github.com/tuoro/Smart-bill-manager/compare/v0.2.0...v0.2.1
[0.2.0]: https://github.com/tuoro/Smart-bill-manager/compare/v0.1.0...v0.2.0
[0.1.0]: https://github.com/tuoro/Smart-bill-manager/releases/tag/v0.1.0
