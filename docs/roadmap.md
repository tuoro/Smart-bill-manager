# 路线图

状态：M0、M1、M2、M3 已完成；M4 首切片“确定性 Fact 洞察与筛选查询”已完成，下一断点为完整备份恢复演练
原则：每个里程碑独立验收，未通过不得提前进入下一阶段

## M0：设计基线

目标：把产品与架构共识变成可执行、可复审的规范。

完成日期：2026-08-27。全部退出门禁已通过，证据见 `docs/m0-evidence.md`；M0 完成不自动启动 M1。

交付物：

- 根 `AGENTS.md`；
- 产品、范围、验收、架构、AI、数据、UI/UX 文档；
- 已批准且可重复执行的量化指标；
- 两套结构不同的视觉方向、已冻结的 02「国内大厂中后台」、四个代表页面和技术验收证据；
- 三项核心架构决策记录。

完成门禁：已通过。门禁定义见 `docs/scope.md` 的 M0 退出条件和 `docs/acceptance.md` 的 M0 证据要求。

## M1：AI 收件箱

目标：用全新架构完成“上传 -> AI 票面原文 JSON -> 本地确定性 Claim -> 校验 -> 人工审核 -> Fact”的第一条垂直链路。

启动日期：2026-08-27。完成日期：2026-08-30。当前状态：已完成。

2026-08-29 验收进度：除模型业务质量外，既有实现、E2E、UI/可访问性、性能、内存、生产镜像与 M1 备份恢复门禁曾通过；架构重构后必须重新执行受影响门禁。历史英文纯合成集只保留传输、Schema、计分与失败路径诊断意义，模型质量边界已重置为中文真实场景。中文真实开发集 `m1-real-dev-v1` 已在本地隔离区冻结，包含 16 份可追溯真实图片（Payment/Receipt 11、Invoice 5），与四个历史合成集图片哈希零重合；产品默认时区为 `Asia/Shanghai`。产品负责人授权后，`qwen3-vl-30b-a3b-instruct` 已使用 Prompt v6 完成真实预检。首轮发现 JPEG 经 Go 重新编码为 16 位 PNG、导致 Provider 在 HTTP 200 下读不到像素；该问题已在唯一规范化位置修复为 `document-normalize/2`：先转换到 8 位 RGBA 像素缓冲再编码为 PNG，并由同像素受控探针、回归测试和纠正运行的 16 / 16 个实际输入页共同验证。纠正预检仍只有 2 / 16 完成、Claim 契约 0 / 16，最终为 11 个 Schema 失败和 3 个 Provider 超时，判定未达到且不接近门槛。

随后完成 Prompt v7 JSON Object 契约修复：业务提取不再重复发送完整投影 Schema，明确既有 `schema_version` 常量；能力检测保留投影 Schema，但把视觉探针收敛为合法短 Envelope。无隐私两样本实际候选重复 4 / 4 通过 JSON、Provider Schema 与权威 Schema；16 张纯合成技术预检有 15 / 16 产生 Claim、15 / 15 通过 Schema、0 个超时，证明传输链路已显著恢复。产品负责人再次授权后，同一候选从全新空库完成 16 份中文真实开发样本预检：15 / 16 产生 Claim、15 / 15 Schema 有效，唯一终止为一次 Provider 超时；但完整 Claim 契约仍为 0 / 16，分类、金额、日期、名称、关键证据和缺失/冲突召回分别为 62.5%、71.43%、75%、68.75%、64.91% 和 73.91%，判定未达到且不接近门槛。下一步只做本地失败类别分析、下一版 Prompt 设计和无隐私契约验证；再次发送真实开发图片仍需当次授权，在真实预检达到或接近门槛前不启动至少 100 样本真实发布运行。证据见 `docs/m1-evidence.md`，M1 继续保持进行中且未进入 M2。

产品负责人随后逐图复核并纠正样本定义：微信支付与支付宝只接受单笔完整支付/账单详情页，打码、模糊、列表、通知卡片、优惠页、扣费凭证、教程/文章拼图和第三方浮层全部排除，实体小票当前不进入 M1；发票只保留无遮挡的完整真实发票。按新边界，v1 的 16 张只有 1 张支付宝详情和 5 张发票可进入 v2 候选，10 张被排除，v1 与其 Prompt v6/v7 结果降级为技术诊断，不再用于业务质量结论或 Prompt 语义调优。下一步按 5 张微信详情、5 张支付宝详情和 6 张发票补齐并冻结 `m1-real-dev-v2`；冻结前不调用 Provider、不启动 100 样本运行。M1 继续保持进行中且未进入 M2。

公开来源补样随后完成：新增 5 张微信单笔详情、4 张支付宝单笔详情和 1 张完整发票，与 v1 沿用候选合并后达到 16 / 16（5 / 5 / 6，实体小票 0）。产品负责人批准全部候选后，完整私有标注、来源回溯、权限、图片签名、发票算术、哈希唯一性及相对历史合成集和 v1 排除样本零重合检查均通过，`m1-real-dev-v2` 已以 SHA-256 `2fe2badb59ac10e11d1573c3ddc5182835afc2463e7e3a5e3d07dd006b03ed15` 冻结。

同日获得 Provider 主机与 16 份样本的当次授权后，v2 使用既有 Qwen3-VL、Prompt v7、Schema v2、输入规范化 v2 和单次 Schema 重试完成隔离预检：13 / 16 形成 Claim，13 / 13 Schema 有效，但完整 Claim 契约 0 / 16；分类、金额、发票号、日期、名称、关键证据、缺失/冲突召回分别为 75%、75%、66.67%、75%、59.09%、82.14%、40%。结果未达到且不接近门槛；M1 继续质量修复，不降低指标、不进入 M2，也不申请或启动 100 样本真实运行。

基于该运行的私有、无字段值失败矩阵，Prompt v8 收紧了裸货币符号、商户相对方、发票号码与购销角色、项目全名、行级可选字段、证据完整性及单根闭合 JSON 规则，并从 8,512 bytes 收敛至 7,530 bytes。产品负责人随后授权同一 Provider 处理恰好 6 份 failure-driven canary；结果 0 / 6 形成 Claim：5 份跨 Payment/Invoice 样本统一在两次尝试后因 `/fields#type` 失败，1 份被 Provider 拒绝。代码复核确认 v8 漏掉了 `fields` 和 `document_issues` 必须为数组的显式约束，属于我们自己的 Prompt 契约回归，不扩大到 16 或 100 份。

失败 v8 身份保持不变。Prompt v9 仅补回数组容器约束，长度 7,752 bytes，Schema、重试、输入规范化、默认时区、语义规则和指标均未变化。从 v8 结构失败中冻结的微信、支付宝和发票各 1 份共 3 份 canary，SHA-256 为 `bec9dcaec7c9530623ced4467d2c0647055ae4c4d15add28e211600158b55b0f`。产品负责人按同一 Provider 主机和恰好 3 份样本逐次授权后，v9 执行 5 次提取请求仍为 0 / 3 Claim：2 次 60 秒 Provider 超时、2 次约 14–16 KB 后无效 JSON、1 次通过 Provider Schema 后在两个 Canonical Field 节点失败。旧的 `/fields#type` 未复现，但门禁没有通过，未扩大到 6、16 或 100 份。

Prompt v10 据此增加精确 present/absent Field 模板、固定 Payment 恰好 8 字段，并把每个 present Field 限制为恰好 1 个完整逐字 evidence；模型、Schema、重试、超时、输入规范化、默认时区和指标均不变。任务说明为 7,812 bytes，同 3 份失败样本已确定性冻结为 `m1-real-dev-v2-prompt-v10-canary`，SHA-256 为 `03a794d5ddd0c53ade849d87185242057745bcbc305b090ed4b4bade1f4d2bf3`。本地 Go、评分器、冻结和未授权守卫已通过；生产镜像 `sha256:0ba80038ae2de96d8ffde6ff88e06cfbcff4dcc349b1b0ddfd86999000d1d0f9` 仅含 16 个生产文件且密钥零命中。v10 从未调用 Provider，现已冻结并停止，不再申请或执行该 canary。

产品负责人随后指出 Observation + 重型 Claim Assembler 把系统退化成“模型只做 OCR、本地重新理解账单”，与产品初衷不符，并批准按本次要求修正。当时活动基线为 `bill-extract/1 -> bill-extraction/1 -> bill-extraction-provider/1 -> claim-mapper/1 -> document-claim/1`：模型直接输出带类型的 Payment/Invoice 业务 JSON、归一化值、来源和发票明细分组；金额使用主币单位精确十进制字符串。本地 Mapper 只做结构搬运、null/证据/顺序映射及精确 minor-unit 换算，不解析图片原文、不推断字段。旧 Observation、重型 Assembler 和旧直接 Claim Provider 均无兼容入口。最终指标、安全边界、人工审核和 Fact 门禁不变；下一步先按新契约冻结中文真实调优集，任何真实图片 Provider 预检仍需新的主机与样本数逐次授权，未达到最终门槛前不进入 M2。

产品负责人随后明确要求立即测试。同一批 16 份已批准原图在不修改像素的前提下按新链路冻结为 `m1-real-dev-v3`，SHA-256 为 `0326adc8dab5473faed732e20080d7e0578d7116d46a62f63f3729277c814626`，并使用当前阿里云 Qwen3-VL、`json_object`、temperature 0 与单次 Schema 重试完成隔离预检。结果 16 / 16 进入模型阶段但 0 / 16 通过 Provider Schema、0 / 16 Claim、0 Fact；15 份在两次尝试后 Schema 失败，1 份被 Provider 拒绝。Payment 统一出现 `amount` 类型错误，被 Provider 接受的 Invoice 统一出现逐字段类型与额外属性错误，证明当前阻塞点是模型输出与 `{value, source}` 逐字段包装契约的系统性形状不匹配，而不是某一图片类别的识别质量。下一步只在无隐私样本上修正模型侧输出形状；不恢复本地重型解析、不降低指标、不启动 100 样本、不进入 M2。

产品负责人随后批准彻底替换该包装契约。当前唯一活动基线更新为 `bill-extract/2 -> bill-extraction/2 -> bill-extraction-provider/2 -> claim-mapper/2 -> document-claim/2`：模型返回自然 Payment/Invoice 标量、独立 Evidence、明细和 `other_fields`；Envelope Schema 只硬校验 JSON 与根身份，字段类型、金额、Evidence、业务区段和补充字段问题形成 blocked Claim 并保留其他正确字段。Mapper 只做路径/Evidence/顺序、额外键收纳和精确 minor-unit 换算，不解析证据原文。额外字段只供审核，不进入 Fact。v1 活动类型、逐字段包装和数据库约束已被移除，无兼容分支。相同 16 份原图/标签已在本地隔离区首次发布为 `m1-real-dev-v4`，SHA-256 为 `cd96056be80b4670c7a315ddcdb37dc5f6a015367013be9bd7336a967157c610`；本轮未调用 Provider。

2026-08-30，v2 受影响的软件门禁已全部重新验收通过：后端全量测试和静态检查通过，覆盖率分别为 85.40% 与 71.61%，关键不变量 38 / 38；Web 完整检查、生产构建、隔离 Compose E2E 13 / 13、响应式与 reflow 各 16 / 16、Lighthouse 12 / 12、10,000 Facts 性能、50 作业内存趋势、独立备份恢复以及生产镜像安全审计均通过。无效的浏览器沙箱、进程采样和租约编排运行未计入结果并已独立重跑。本轮未调用真实 Provider；M1 剩余边界仅为 v4 真实小样本质量预检、缺失/冲突召回样本和达到门槛后的 100 样本三次发布评测，因此仍保持进行中且不得进入 M2。

同日取得明确继续授权后，v4 使用当前阿里云 `qwen3-vl-30b-a3b-instruct`、`json_object`、Prompt/Schema/Mapper v2、输入规范化 v2 和单次 Schema 重试完成 16 份真实预检：9 / 16 形成 Claim，9 / 9 通过 v2 Schema，7 份因 5 次 Provider 超时、1 次无效 JSON 和 1 次 Provider 拒绝在模型阶段终止；完整 Claim 契约仍为 0 / 16。分类、金额、发票号、日期、名称、关键 Evidence 和清单断言分别为 56.25%、50%、33.33%、56.25%、31.82%、59.52% 和 62.26%，没有直接 Fact。v2 已解决旧包装的整份结构阻塞，但完成率、语义和 Evidence 均不接近门槛；不得启动 100 样本，M1 继续保持进行中且未进入 M2。

产品负责人随后授权智谱 `glm-5.3-flash` 对同一 v4 做单变量 A/B，除 Provider 和模型外全部配置不变。智谱得到 11 / 16 Claim、11 / 11 Schema、0 / 16 完整 Claim，5 份全部因 Provider 超时终止；分类、金额、发票号、日期、名称、关键 Evidence 和清单断言分别为 68.75%、68.75%、16.67%、62.5%、45.45%、64% 和 62.26%，没有直接 Fact。它在完成率及多数语义指标上优于 Qwen、且没有无效 JSON 或 Provider 拒绝，但发票号更差，11 个 Claim 仍全部被阻断。智谱只成为下一 Prompt 的优先候选，未达到生产切换或 100 样本条件；M1 继续保持进行中且未进入 M2。

产品负责人随后授权使用同一智谱模型对 v4 中五份发票做最小归因诊断。当前混合 Prompt 完成 3 / 5；在三个完成响应中，业务值和 Evidence 平均分别为 83.89% 与 71.67%，完整契约代理仍为 0 / 5。发票专用直出仅完成 1 / 5，但该唯一响应的业务值、Evidence 和完整契约代理均通过；其余四份超时。同模型“视觉转录 -> 语义整理”最终完成 0 / 5：两份第一阶段超时、三份第二阶段超时。该诊断只证明当前 60 秒边界内同模型顺序拆分增加了失败面；它没有测试专门 OCR/Document Parser，也因固定调用顺序、原始图片输入、五份单明细小样本而不能形成生产架构结论。活动架构、指标和门禁均未改变，仍不得启动 100 样本或进入 M2。

产品负责人随后要求用阿里云百炼官方 Key 和 `qwen3.8-flash` 复测同一五份发票。官方 JSON 能力探针通过后，显式关闭思考模式并保持其余诊断配置不变。当前混合直出 5 / 5 完成、平均 7.56 秒，业务值和全部期望 Evidence 分别为 90.52% 与 79.36%，但完整契约代理仍为 0 / 5；业务值错误集中在发票号、销售方和一处总额，币种 Evidence 则 5 / 5 未满足。发票专用直出为 83.03%/83.03%，同模型两阶段业务值为 73.88%，都没有优于当前直出。两阶段 Evidence 0% 受诊断 Prompt v1 未完整声明 Evidence 元素形状影响，不计作架构否定证据。官方 Qwen 在该小组上没有超时且整体优于智谱，成为下一 Prompt 的优先候选，但仍未达到生产切换、100 样本或 M2 条件。

随后以同一官方 Qwen、同一五份发票和固定 Provider 配置，对当前 `bill-extract/2` 与只强化 Evidence 形状、币种证据、发票号及购销方复核的 `bill-extract/3` 候选做单变量 A/B。本轮 v2 完成 4 / 5、完整契约 0 / 5；v3 完成 5 / 5、严格形状 5 / 5、业务值 86.18%、Evidence 84.18%、完整契约 2 / 5。v3 把币种 Evidence 失败从 5 次降到 1 次并消除三次总额失败，但仍有发票号和销售方错误，且一份样本新增明细金额、税额回退；前次 v2 业务值 90.52% 也高于本轮 v3。故 v3 只证明契约方向有效，不激活、不进入 16 或 100 样本，M1 继续保持进行中。

官方参数核查随后确认，一份 4032×3024 屏摄发票超过 Qwen3.8 默认 2,621,440 像素上限；同时默认 `presence_penalty=1.5` 不利于金额与币种复用同一 Evidence。把高分辨率与 `presence_penalty=0` 固定到两侧后，v3 为 5 / 5 完成、86.18% 业务值、86.18% Evidence、2 / 5 完整契约；更细的 v4 虽修复该屏摄样本的明细金额/税额，却让两份原本完整样本新增总额错误，完整契约降至 0 / 5。低档思考模式下 v3 又降至 4 / 5 完成、0 / 5 完整契约，平均完成耗时由 7.54 秒增至 18.95 秒。v4 与思考模式均被否决；Prompt 修补在此停止，下一步必须重新决定模型或视觉输入能力，不能启动 16、100 样本或 M2。

随后以阿里云专用 `qwen3.5-ocr` 对同组五份发票执行默认纯文字转录诊断：5 / 5 完成，平均耗时 3.20 秒；严格 Evidence 原文召回 69.06%，业务值文本召回 81.36%，两项均为 0 / 5 完整。金额、税额和明细文本全部出现，但仍漏币种 3 次、发票号 2 次、销售方 2 次、日期 2 次和购买方 1 次。它没有解决当前最难字段，且仍需第二阶段生成语义 JSON 与 Evidence，因此不加入活动链路；活动 Prompt、Provider、Schema、Mapper 和 M1 门禁均不改变。

产品负责人随后明确保持纯 AI、可替换 Provider 架构，不引入本地 OCR 主链，并授权 `qwen3.5-ocr` 使用现行 `bill-extract/2` 直接输出最终 JSON。五份样本均在一次原样重试后仍为 `invalid_json`，0 / 5 通过传输门禁，因而没有可计分的业务值或 Evidence；工具没有剥离 Markdown、抽取 JSON 片段或修复字段。该模型在当前产品边界下不能替换现有 Provider，已否决且不再调 Prompt；后续只评估原生支持稳定 JSON/Schema 输出的更强视觉模型，活动架构和 M1 门禁不变。

随后按产品负责人提出的“中文字段清单 + 本地薄拼装”方案，先以官方 `qwen3.8-flash` 和当前最佳高分辨率非思考配置测试同组五份发票。模型字段清单与拼装后的最终根结构均为 5 / 5 有效，证明该简单重复记录契约可执行；但业务值仍为 86.18%，与直出 v3 完全相同且失败路径完全一致，Evidence 反而由 86.18% 降至 79.03%，完整契约由 2 / 5 降至 0 / 5。它消除了结构负担却没有突破视觉语义上限，因此不激活、不再针对 Qwen3.8 修补；该 Claim 契约只保留为未来更强视觉模型的候选资格赛接口。

产品负责人随后授权同组五份发票执行本地图像输入 A/B。`document-normalize/3-candidate-a` 不做 OCR、模板或字段解析，只对四份低分辨率图进行 2× Lanczos 与轻度反锐化，并对唯一高分辨率屏摄图按像素统计启用受限亮度对比度。相同 `qwen3.8-flash`、`bill-claims-cn/1`、薄 Assembler 和 Provider 配置下，原图/候选均 5 / 5 完成且 1 / 5 完整契约；业务值由 84.52% 升至 88.00%，Evidence 由 80.70% 升至 82.52%，逐样本为改善 3、不变 1、回退 1。候选修复一处发票号等少量路径，但没有改善销售方或困难屏摄明细，屏摄样本反而新增一处币种 Evidence 回退，输入 token 增加约 29.5%。因此 candidate-a 不激活；只保留低分辨率放大/轻锐化的正向信号，停止屏摄自动对比度分支。活动输入仍为 `document-normalize/2`，不进入 16、100 样本或 M2，本次授权不得复用。

继续授权后发布 `document-normalize/3-candidate-b`：四份低分辨率派生字节与 candidate-a 完全相同，高分辨率屏摄图改为源字节旁路且 A/B 两侧共享一次 Provider 响应。9 个实际路径请求完成后，原图/候选仍均 5 / 5 完成、1 / 5 完整契约，业务值由 86.18% 升至 88.00%，Evidence 由 82.52% 升至 84.52%，逐样本改善 2、不变 3、整体回退 0；同一发票号在两轮中均被增强图修复，但销售方与困难屏摄明细仍未改善，token 增加约 29.5%、平均耗时增加约 10.1%。candidate-b 仅冻结为后续 M1 视觉输入开发基线，不替换活动 `document-normalize/2`；下一决策必须针对通用裁边/多视图或更强模型，不能启动 16、100 样本或 M2，本次授权不得复用。

继续授权后发布 `document-normalize/3-candidate-c`，只把高分辨率屏摄图改成同页全景加四个重叠原像素分块，四份低分辨率派生字节保持不变。10 个 A/B 路径均完成：业务值 82.18%→86.00%、Evidence 76.70%→84.18%、完整契约 1 / 5→2 / 5，逐样本改善 4、不变 1、整体回退 0；但屏摄样本只修复币种，销售方与明细名称、金额、税额仍失败，输入 token 增加约 42.7%、平均耗时增加约 15.8%。新增完整契约来自与 candidate-b 字节相同的低分辨率样本，不能归因于多视图。candidate-c 因此冻结为已验证但未采用的边界证据，不替换 candidate-b 或活动 `document-normalize/2`，不启动 16、100 样本或 M2，本次授权不得复用。

产品负责人确认原图在手机端可清晰阅读后，授权使用相同五份原图直接比较 `qwen3.8-flash` 与 `qwen3.8-max`，不使用任何图像派生物。Max 能力探针一次通过，10 个真实 A/B 路径全部完成：Flash/Max 业务值为 86.18%/86.52%，Evidence 为 79.03%/85.03%，完整契约均为 0 / 5，逐样本 Max 改善 1、不变 2、回退 2。Max 将屏摄样本由 60%/60% 提升到 80%/90%，修复明细金额和税额，却在三份普通发票上统一漏掉非零发票税额；平均耗时约为 Flash 的 2.88 倍。结果证明更强模型能读取原图中的困难信息，但 Max 当前输出行为仍不能替换 Flash；冻结为模型候选，不启动 16、100 样本或 M2，本次授权不得复用。

产品负责人随后只授权通过 Shuai 资格测试 `gpt-5.6-sol + medium`，千问继续引用阿里云官方 Key 的既有结果。目标模型在 Key 的模型目录可见，但 Shuai Chat Completions 连续返回 502，实际可用的 Responses 能力探针通过；五图因此使用同一原图、`bill-claims-cn/1`、同页说明和薄 Assembler，固定 `detail=high`、`store=false`、8,192 输出 token 上限与 120 秒超时。Shuai 不接受 Responses 原生 `json_object`，所以只对 Prompt 要求的 JSON 文字做严格解析，不做响应修复。5 / 5 完成且根结构合法，但业务值/Evidence 均仅 69.70%、完整契约 0 / 5，屏摄样本仅 30%/30%，平均耗时 64.1 秒；明显低于官方 Flash/Max 且更慢。该候选不激活、不扩大到 16 或 100 样本；结论只适用于本次 Shuai 路由和冻结配置，不外推为 OpenAI 官方直连结果。M1 仍进行中且未进入 M2。

产品负责人随后授权官方 `qwen3.8-flash` 用同一五份原图交替比较当前 `bill-extract/2` 与只返回发票业务值的最小中文 `invoice-values-cn/1`。两侧均 5 / 5 完成且输出形状合法；完整契约/最小候选业务值为 88.52%/86.85%，逐样本 4 份持平、1 份候选回退；候选只把平均耗时降低约 76.2%、输出 token 降低约 80.8%。随后按要求进行的不落盘人工检查修正了初始归因：部分总额失败是含税/不含税字段语义未统一，数量格式与可见括号存在数值或文本等价假失败，币种 Evidence 又被完整金额片段匹配过度约束；同时仍有发票号增删零、购销方单字误识别和计算空白单价等真实模型错误。因此最小候选不激活，但也不能据当前分数直接认定模型是主因；下一步先修正评测定义与标签，再重算和决定模型策略，不启动 16、100 样本或 M2，本次授权不得复用。

产品负责人随后批准以“最小中文直接询问所需数字或文本，本地负责其余确定性处理”替换活动链路。当前唯一基线为 `bill-visible-text-cn/1 -> bill-visible-text/1 -> bill-visible-text-provider/1 -> claim-mapper/3 -> document-claim/2`：模型对每个字段只返回 `{text,page}` 或 `null`；本地从同一原文组装 Evidence，并处理批准的币种、金额、日期、时间、时区和数量表示法。发票金额已拆为不含税、税额和含税合计，只有含税合计进入 `total_minor`；空白单价不得计算。评分器现按十进制数量语义、整值外层括号等价及页码一致的最小充分 Evidence 计分，且新增反例自检。旧自然标量、独立 Evidence、`other_fields` / `issues` 和 `claim-mapper/2` 已退出活动链路，无兼容分支；`m1-real-dev-v4` 冻结为历史证据并被当前 Runner 阻止发送。该替换轮次只实施软件与纯合成门禁，没有调用真实 Provider，也没有进入 M2；当时冻结的下一步是首次发布 `m1-real-dev-v5` 并重新取得明确授权。

随后按授权首次发布 `m1-real-dev-v5` 并由阿里云官方 `qwen3.8-flash` 执行 16 份真实预检。15 / 16 形成 Claim、15 / 15 根 Schema 有效；10 份支付金额原文 10 / 10 命中，但模型对票面不可见的独立币种均返回空，本地安全规则因此不换算金额。发票号 3 / 6、支付商户 6 / 10，说明最小契约改善了结构、金额原文与日期，却没有解决关键名称和号码误识别。v5 复制标签还漏列固定 `supplementary_fields`，其 0% 完整 Claim 率不可用于模型排名。v5 已冻结并被 Runner 阻止重复发送；下一步先决定不可见币种的可审计来源并发布独立修正版，不启动 100 样本或 M2。

2026-08-30，产品负责人基于真实上传会保证清晰、完整、无遮挡且关键字段可直接人工辨读，批准停止让当前测试图的高精度纠缠阻塞其余 M1 开发。原准确率目标和 100 份三次正式评测不被删除或改写，统一移至 M4 首次正式发布门禁；v5 未达到这些数值的事实继续保留。当前安全闭环证据为：16 / 16 个任务均形成 Claim 或明确 Provider 失败、15 / 15 本地接受根对象通过 Schema、AI 直接 Fact 为 0，且完整软件、租户、审核、幂等、性能、可访问性、备份恢复与生产镜像门禁已经通过。这项门禁调整本身不代表模型质量通过，也不单独授权进入 M2。

产品负责人随后确认模型正确率专项安排在全部功能开发完成后处理，并授权继续推进。M1 收口复核确认范围内功能无缺口、M2 实现为零；当前活动链路下 Go 全量测试、`go vet`、构建和 Web 全量检查通过，领域/应用层与基础设施/传输层覆盖率分别为 86.57% 和 71.60%，关键不变量 38 / 38。结合既有 Compose E2E、UI/可访问性、性能、内存、备份恢复和生产镜像证据，`docs/scope.md` 的 M1 退出条件全部满足，M1 据此完成。模型历史失败继续保留为 M4 工作；M2 仍未启动且未获授权。

关键交付：

- `apps/api`、`apps/web`、`contracts`、`infra` 和 `tests` 基础结构；
- 新数据库 `0001`；
- 空数据库专用 `bootstrap-owner` 与四角色最小权限矩阵；
- Payment/Invoice 最小事实模型；
- Payment/Invoice 最小一对一关联建议与人工确认；
- Provider 能力检测、BillExtractor、OpenAI-compatible Adapter 和本地薄 Claim Mapper；
- 审核工作台；
- 纯合成契约、集成与 E2E，以及本地隔离的中文真实场景模型评估；
- 新系统 Compose 冒烟与备份恢复基线。

## M2：发票与关联

目标：在 M1 最小一对一关联之上，完善发票、明细、重复检测以及复杂支付关联。

启动日期：2026-08-30。完成日期：2026-08-31。当前状态：已完成。产品负责人要求模型正确率专项留到全部功能开发完成后处理。

首个切片“支付—发票金额分配”已于 2026-08-30 完成：M1 的全额一对一候选已替换为同币种余额候选，人工输入的正整数最小单位分配计划在一个事务中创建 Fact、决定、不可变 Link 和审计事件；列表余额状态、幂等计划绑定、并发超分配保护和删除后余额恢复均已通过验收。证据见 `docs/m2-evidence.md`。该结果不包含独立重分配工作流、批量上传、复杂跨页或近似判重。

第二个切片“近似文件、字段组合和跨页重复检测”已于 2026-08-30 完成：现有规范化页产生版本化双视觉指纹，整份/跨页/字段组合信号形成不可变审核候选，用户确认 Fact 前必须逐项明确保留独立记录，确认事务重检候选集合；数据库、API、Web UI、55 / 55 关键不变量、覆盖率和 11 / 11 浏览器组件场景均通过。证据见 `docs/m2-evidence.md` 和 `tests/evidence/m2/duplicate-detection-gate-summary.json`。该切片未调用模型判断重复，也不承担跨页明细重建。

第三个切片“复杂多页 PDF 的跨页明细重建与分页审阅”已于 2026-08-30 完成：保持当前最小中文多模态契约不变，以模型已有逻辑 item 分组和逐字段页码为输入，本地只从当前 Claim Evidence 派生完整页序、连续页跨度和顺序校验；租户隔离的规范化页 API、分页审核 UI、60 / 60 关键不变量、两层覆盖率及 13 / 13 浏览器组件场景均通过。不增加 OCR、文字猜接、第二数据源或真实 Provider 调用，证据见 `docs/m2-evidence.md` 和 `tests/evidence/m2/cross-page-review-gate-summary.json`。

第四个切片“多 Document 批量上传与逐项反馈”已于 2026-08-31 完成：Web 严格按选择顺序编排现有单 Document 上传端点，每项独立入队、判重或失败；中间失败继续、超限显式反馈、键盘与响应式回流、61 / 61 关键不变量、两层覆盖率和 14 / 14 浏览器组件场景均通过。不新增 Batch 实体、服务端批量端点、数据库迁移、批次回滚或第二数据源；证据见 `docs/m2-evidence.md` 和 `tests/evidence/m2/batch-upload-gate-summary.json`。

第五个切片“已确认 Fact 之间的独立补充分配、撤销或替换工作流”已于 2026-08-31 完成：完整期望计划、乐观快照、幂等重放、不可变 Adjustment/Link 来源、权限、独立 Web 页面和并发回滚均通过验收。最终门禁为 72 / 72 关键不变量、85.86% / 73.01% 两层覆盖率、21 项 Web 单测和 18 / 18 浏览器场景；证据见 `docs/m2-evidence.md`、`tests/evidence/m2/allocation-adjustment-gate-summary.json` 和 `tests/evidence/m2/m2-closure-gate-summary.json`。M2 据此收口。

关键交付：

- 在 M1 对不确定跨页内容一律阻断的基础上，实现复杂多页 PDF 的跨页明细重建与分页审阅；
- 在 M1 精确文件哈希与规范化发票号重复规则之上，增加近似文件、跨页和字段组合重复检测（第二切片已完成）；
- 一对多、多对一、部分金额和并发冲突保护（首切片已完成）；
- 关联原因、冲突和审核证据；
- 批量上传作为独立验收项（第四切片已完成）；
- 已确认 Fact 的原子补充分配、撤销和替换（第五切片已完成）。

## M3：邮箱、行程与报销

目标：将邮件和差旅资料纳入同一 Source/Claim/Fact 模型。

完成日期：2026-08-31。三个切片已分别按 ADR-0014、ADR-0015 与 ADR-0016 完成并通过本地验收；最终门禁为 Go 全量测试/静态检查/构建、Web 完整检查、29 / 29 浏览器组件场景、104 / 104 关键不变量及两层覆盖率全部通过。真实邮箱账号、凭据、网络连接、轮询与实联调继续停在授权门禁外；完整证据见 `docs/m3-evidence.md` 与 `tests/evidence/m3/m3-closure-gate-summary.json`。

关键交付：

- 邮箱来源、邮件和附件归档；
- 行程事实与单据归属建议；
- 报销状态和确定性政策；
- 缺少发票、金额冲突和重复报销提示。

## M4：洞察、加固与首个正式发布

目标：完善基于 Fact 的查询与运营质量，发布独立新系统。

当前进展：首切片“确定性 Fact 洞察与筛选查询”已按 ADR-0017 完成并通过本地验收，基于当前 Fact 的同快照汇总、稳定分页、权限和 Web 工作区均已落地，证据见 `docs/m4-evidence.md`。下一切片先冻结 1,000 个纯合成 Document 的完整备份恢复演练，再按文档顺序进入运行质量与本地发布准备。自然语言查询继续留在 Backlog；真实模型正确率正式评测、真实外部系统联调和生产发布继续停在单独授权门禁前。

关键交付：

- 数据洞察和可解释聚合；
- 完整备份恢复演练；
- 性能、安全、可访问性和生产镜像验收；
- 运维文档、故障诊断和发布证据；
- 独立最终复审。

M4 不包含旧数据迁移或旧系统切换。旧版本继续作为历史 Release 存在，但不提供兼容承诺。

## Backlog

以下内容只有在 M4 完成后才能重新评估：

- 自动审核或自动入账；
- 自然语言查询助手；
- 独立 Worker 服务；
- PostgreSQL 和对象存储实现；
- 托管 SaaS、计费和配额；
- 银行或会计软件集成；
- 原生移动端。

## 里程碑模板

启动任何新里程碑前必须补齐：

1. 目标；
2. 范围内；
3. 范围外；
4. 用户场景；
5. 功能验收；
6. 质量与安全验收；
7. 备份、恢复或失败处理；
8. 验收证据；
9. 退出条件。
