# M1 证据索引

`gate-summary.json` 是当前 M1 收口摘要，活动身份固定为 `bill-visible-text-cn/1 -> bill-visible-text/1 -> bill-visible-text-provider/1 -> claim-mapper/3 -> document-claim/2`。

以 `historical-` 开头的 JSON 是被替代 Prompt、Observation 或 Assembler 链路的冻结历史证据，只用于解释当时结论，不是当前运行时契约、当前门禁状态或兼容入口。完整叙述与证据边界见 `docs/m1-evidence.md`。
